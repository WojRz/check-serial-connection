[project]
name = "check-serial-connection"
version = "0.1.2"
description = ""
authors = [
    {name = "wrzezniczak",email = "wojtek.sqwarku@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click (>=8.1.8,<9.0.0)",
    "pyserial (>=3.5,<4.0)",
]

[tool.poetry.scripts]
check-serial-connection = "check_serial_connection.__main__:main"

[tool.poetry.group.test.dependencies]
pytest = "^8.3.4"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
